# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# 环境与密钥
.env

# IDE
.idea/
.vscode/

# 数据文件（原件另行备份，不进 git）
data/
*.pdf

# Source 登记：只保留 source.yaml，冻结原件（original）不入库（只留指纹 checksum）
sources/*/original
!sources/*/source.yaml

# Pipeline 生成物（knowledge/provenance/summary 等）不入库，可由源码复现
sources/*/output/
